// GitHub Pages 部署后可在这里填入固定服务器地址，例如：
// export const DEFAULT_SERVER = 'wss://example.com/ws';
export const DEFAULT_SERVER = '';

export const CLIENT_DEFAULTS = Object.freeze({
  nearDistance: 16,
  farDistance: 1024,
  shadowDistance: 4,
  visualPreset: 'soft',
  gameMode: 'survival',
  singleplayerSlot: 1,
});
